name: Core Deployment Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: '@craftfosslabs/dragifyjs Build'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: read
    steps:
      - name: 'Checkout repository'
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Clear npm cache
        run: npm cache clean --force

      - name: 'Install dependencies with npm'
        run: npm install

      - name: 'Build CLI package'
        run: |
          cd packages/cli
          npm install
          npm run build  # Run build script specific to the CLI package
          cd ../..

      - name: 'Restore changes in package-lock.json'
        run: git restore package-lock.json

      - name: 'Set Git user name and email'
        run: |
          git config --local user.email "github-actions@github.com"
          git config --local user.name "GitHub Actions"

  create-github-release:
    name: Create GitHub release document and publish to npm
    runs-on: ubuntu-latest
    permissions:
      contents: write
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x

      - name: Clear npm cache
        run: npm cache clean --force

      - name: Install dependencies with npm
        run: npm install

      - name: Semantic Release and npm release
        run: |
          cd packages/cli
          npm install --frozen-lockfile
          npm run build  # Ensure the CLI package is built before release
          npx semantic-release
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.ACTION_GITHUB }}
